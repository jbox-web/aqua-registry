# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
# GENERATED by scripts/build-registry.sh — do not edit by hand.
packages:
  - type: github_release
    repo_owner: axllent
    repo_name: mailpit
    description: An email and SMTP testing tool with API for developers
    asset: mailpit-{{.OS}}-{{.Arch}}.{{.Format}}
    format: tar.gz
    overrides:
      - goos: windows
        format: zip
  - type: github_release
    repo_owner: composer
    repo_name: composer
    description: Dependency Manager for PHP
    asset: composer.phar
    checksum:
      type: http
      url: https://getcomposer.org/download/{{trimV .Version}}/composer.phar.sha256sum
      algorithm: sha256
    files:
      - name: composer
        src: composer.phar
    supported_envs:
      - darwin
      - linux
      - windows
  - type: github_release
    repo_owner: elbywan
    repo_name: crystalline
    description: A Language Server Protocol implementation for Crystal
    asset: crystalline_{{.Arch}}-{{.OS}}.{{.Format}}
    format: gz
    replacements:
      amd64: x86_64
      darwin: apple-darwin
      linux: unknown-linux-musl
    files:
      - name: crystalline
    supported_envs:
      - linux/amd64
      - darwin
  - type: github_release
    repo_owner: getsentry
    repo_name: sentry-cli
    description: A command line utility to work with Sentry
    asset: sentry-cli-{{.OS}}-{{.Arch}}
    format: raw
    replacements:
      amd64: x86_64
      arm64: aarch64
      darwin: Darwin
      linux: Linux
    overrides:
      - goos: darwin
        replacements:
          arm64: arm64
    supported_envs:
      - darwin
      - linux
  - type: github_release
    repo_owner: go-acme
    repo_name: lego
    description: Let's Encrypt/ACME client and library written in Go
    asset: lego_{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
    format: tar.gz
    checksum:
      type: github_release
      asset: lego_{{trimV .Version}}_checksums.txt
      algorithm: sha256
    overrides:
      - goos: windows
        format: zip
  - type: github_release
    repo_owner: golangci
    repo_name: golangci-lint
    description: Fast linters runner for Go
    asset: golangci-lint-{{trimV .Version}}-{{.OS}}-{{.Arch}}.{{.Format}}
    format: tar.gz
    checksum:
      type: github_release
      asset: golangci-lint-{{trimV .Version}}-checksums.txt
      algorithm: sha256
    files:
      - name: golangci-lint
        src: golangci-lint-{{trimV .Version}}-{{.OS}}-{{.Arch}}/golangci-lint
    overrides:
      - goos: windows
        format: zip
  - type: github_release
    repo_owner: goreleaser
    repo_name: goreleaser
    description: Release engineering, simplified
    asset: goreleaser_{{.OS}}_{{.Arch}}.{{.Format}}
    format: tar.gz
    replacements:
      386: i386
      amd64: x86_64
      darwin: Darwin
      linux: Linux
      windows: Windows
    checksum:
      type: github_release
      asset: checksums.txt
      algorithm: sha256
    overrides:
      - goos: windows
        format: zip
  - type: github_release
    repo_owner: grafana
    repo_name: loki
    description: Like Prometheus, but for logs
    asset: loki-{{.OS}}-{{.Arch}}.zip
    format: zip
    checksum:
      type: github_release
      asset: SHA256SUMS
      algorithm: sha256
    files:
      - name: loki
        src: loki-{{.OS}}-{{.Arch}}
  - name: grafana/promtail
    type: github_release
    repo_owner: grafana
    repo_name: loki
    description: Agent which ships the contents of local logs to a Loki instance
    asset: promtail-{{.OS}}-{{.Arch}}.zip
    format: zip
    checksum:
      type: github_release
      asset: SHA256SUMS
      algorithm: sha256
    files:
      - name: promtail
        src: promtail-{{.OS}}-{{.Arch}}
  - type: github_release
    repo_owner: Infisical
    repo_name: cli
    description: The official CLI of Infisical
    asset: cli_{{trimV .Version}}_{{.OS}}_{{.Arch}}.tar.gz
    checksum:
      type: github_release
      asset: checksums.txt
      algorithm: sha256
    files:
      - name: infisical
    supported_envs:
      - darwin/amd64
      - darwin/arm64
      - linux/amd64
      - linux/arm64
      - linux/386
      - windows/amd64
  - type: github_release
    repo_owner: jbox-web
    repo_name: apt-larder
    description: "apt-larder - stock your packages, serve them fresh"
    asset: apt-larder-{{.OS}}-{{.Arch}}
    format: raw
    checksum:
      type: github_release
      asset: "{{.Asset}}.sha256"
      algorithm: sha256
    supported_envs:
      - darwin
      - linux
  - type: github_release
    repo_owner: jbox-web
    repo_name: docker-health
    description: docker-health - a simple healthcheck tool for Docker containers
    asset: docker-health-{{.OS}}-{{.Arch}}
    format: raw
    checksum:
      type: github_release
      asset: "{{.Asset}}.sha256"
      algorithm: sha256
    supported_envs:
      - darwin
      - linux
  - type: github_release
    repo_owner: jbox-web
    repo_name: envtpl.cr
    description: envtpl - render a template with environment variables
    asset: envtpl-{{.OS}}-{{.Arch}}
    format: raw
    files:
      - name: envtpl
    checksum:
      type: github_release
      asset: "{{.Asset}}.sha256"
      algorithm: sha256
    supported_envs:
      - darwin
      - linux
  - type: github_release
    repo_owner: jbox-web
    repo_name: netbox-extractor
    description: netbox-extractor - extract data from Netbox
    asset: netbox-extractor-{{.OS}}-{{.Arch}}
    format: raw
    checksum:
      type: github_release
      asset: "{{.Asset}}.sha256"
      algorithm: sha256
    supported_envs:
      - darwin
      - linux
  - type: github_release
    repo_owner: jbox-web
    repo_name: squarectl
    description: squarectl - a CLI to manage jbox-web infrastructure
    asset: squarectl-{{.OS}}-{{.Arch}}
    format: raw
    checksum:
      type: github_release
      asset: "{{.Asset}}.sha256"
      algorithm: sha256
    supported_envs:
      - darwin
      - linux
  - type: github_release
    repo_owner: jbox-web
    repo_name: stacker
    description: stacker - a CLI to manage jbox-web stacks
    asset: stacker-{{.OS}}-{{.Arch}}
    format: raw
    checksum:
      type: github_release
      asset: "{{.Asset}}.sha256"
      algorithm: sha256
    supported_envs:
      - darwin
      - linux
  - type: github_release
    repo_owner: mikefarah
    repo_name: yq
    description: yq is a portable command-line YAML processor
    asset: yq_{{.OS}}_{{.Arch}}
    checksum:
      type: github_release
      asset: checksums
      algorithm: sha256
    supported_envs:
      - darwin
      - linux
      - windows
  - type: github_release
    repo_owner: nginx
    repo_name: nginx-prometheus-exporter
    description: NGINX Prometheus Exporter for NGINX and NGINX Plus
    asset: nginx-prometheus-exporter_{{trimV .Version}}_{{.OS}}_{{.Arch}}.tar.gz
    checksum:
      type: github_release
      asset: nginx-prometheus-exporter_{{trimV .Version}}_checksums.txt
      algorithm: sha256
    files:
      - name: nginx_exporter
        src: nginx-prometheus-exporter
  - type: github_release
    repo_owner: oliver006
    repo_name: redis_exporter
    description: Prometheus Exporter for Valkey & Redis Metrics. Supports Valkey 9.x, 8.x, 7.x and various Redis versions
    asset: redis_exporter-v{{trimV .Version}}.{{.OS}}-{{.Arch}}.tar.gz
    checksum:
      type: github_release
      asset: sha256sums.txt
      algorithm: sha256
    files:
      - name: redis_exporter
        src: redis_exporter-v{{trimV .Version}}.{{.OS}}-{{.Arch}}/redis_exporter
  - type: http
    repo_owner: openobserve
    repo_name: openobserve
    description: Cloud native observability platform (logs, metrics, traces)
    url: https://downloads.openobserve.ai/releases/openobserve/v{{trimV .Version}}/openobserve-v{{trimV .Version}}-{{.OS}}-{{.Arch}}.tar.gz
    format: tar.gz
    version_source: github_tag
    files:
      - name: openobserve
    supported_envs:
      - linux
      - darwin
  - type: github_release
    repo_owner: php
    repo_name: frankenphp
    description: The modern PHP app server
    asset: frankenphp-{{.OS}}-{{.Arch}}
    format: raw
    replacements:
      amd64: x86_64
      darwin: mac
    overrides:
      - goos: linux
        replacements:
          arm64: aarch64
    supported_envs:
      - darwin
      - linux
  - type: github_release
    repo_owner: prometheus-community
    repo_name: pgbouncer_exporter
    description: Prometheus exporter for PgBouncer
    asset: pgbouncer_exporter-{{trimV .Version}}.{{.OS}}-{{.Arch}}.tar.gz
    checksum:
      type: github_release
      asset: sha256sums.txt
      algorithm: sha256
    files:
      - name: pgbouncer_exporter
        src: pgbouncer_exporter-{{trimV .Version}}.{{.OS}}-{{.Arch}}/pgbouncer_exporter
  - type: github_release
    repo_owner: prometheus-community
    repo_name: postgres_exporter
    description: A PostgreSQL metric exporter for Prometheus
    asset: postgres_exporter-{{trimV .Version}}.{{.OS}}-{{.Arch}}.tar.gz
    checksum:
      type: github_release
      asset: sha256sums.txt
      algorithm: sha256
    files:
      - name: postgres_exporter
        src: postgres_exporter-{{trimV .Version}}.{{.OS}}-{{.Arch}}/postgres_exporter
  - type: github_release
    repo_owner: prometheus
    repo_name: alertmanager
    description: Prometheus Alertmanager
    asset: alertmanager-{{trimV .Version}}.{{.OS}}-{{.Arch}}.tar.gz
    checksum:
      type: github_release
      asset: sha256sums.txt
      algorithm: sha256
    files:
      - name: alertmanager
        src: alertmanager-{{trimV .Version}}.{{.OS}}-{{.Arch}}/alertmanager
      - name: amtool
        src: alertmanager-{{trimV .Version}}.{{.OS}}-{{.Arch}}/amtool
  - type: github_release
    repo_owner: prometheus
    repo_name: prometheus
    description: The Prometheus monitoring system and time series database
    asset: prometheus-{{trimV .Version}}.{{.OS}}-{{.Arch}}.tar.gz
    checksum:
      type: github_release
      asset: sha256sums.txt
      algorithm: sha256
    files:
      - name: prometheus
        src: prometheus-{{trimV .Version}}.{{.OS}}-{{.Arch}}/prometheus
      - name: promtool
        src: prometheus-{{trimV .Version}}.{{.OS}}-{{.Arch}}/promtool
  - type: github_release
    repo_owner: seaweedfs
    repo_name: seaweedfs
    description: SeaweedFS is a fast distributed storage system for blobs, objects, files, and data lake
    asset: "{{.OS}}_{{.Arch}}.{{.Format}}"
    format: tar.gz
    checksum:
      type: github_release
      asset: "{{.Asset}}.md5"
      algorithm: md5
    files:
      - name: weed
        src: weed
    overrides:
      - goos: windows
        format: zip
    supported_envs:
      - darwin
      - linux
      - windows/amd64
  - type: github_release
    repo_owner: smallstep
    repo_name: certificates
    description: A private certificate authority (X.509 & SSH) & ACME server for secure automated certificate management, so you can use TLS everywhere & SSO for SSH
    asset: step-ca_{{.OS}}_{{.Arch}}.{{.Format}}
    format: tar.gz
    files:
      - name: step-ca
        src: step-ca_{{.OS}}_{{.Arch}}/step-ca
    checksum:
      type: github_release
      asset: checksums.txt
      algorithm: sha256
    overrides:
      - goos: windows
        format: zip
  - type: github_release
    repo_owner: smallstep
    repo_name: cli
    description: A zero trust swiss army knife for working with X509, OAuth, JWT, OATH OTP, etc
    asset: step_{{.OS}}_{{.Arch}}.{{.Format}}
    format: tar.gz
    checksum:
      type: github_release
      asset: checksums.txt
      algorithm: sha256
    files:
      - name: step
        src: step_{{.OS}}_{{.Arch}}/bin/step
    overrides:
      - goos: windows
        format: zip
  - type: github_release
    repo_owner: StackExchange
    repo_name: dnscontrol
    description: Infrastructure as code for DNS
    asset: dnscontrol_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
    format: tar.gz
    checksum:
      type: github_release
      asset: checksums.txt
      algorithm: sha256
    overrides:
      - goos: darwin
        asset: dnscontrol_{{trimV .Version}}_{{.OS}}_all.{{.Format}}
      - goos: windows
        format: zip
  - type: github_release
    repo_owner: webdevops
    repo_name: go-crond
    description: "Cron daemon written in golang (for eg. usage in docker images)"
    asset: go-crond.{{.OS}}.{{.Arch}}
    format: raw
    supported_envs:
      - darwin
      - linux
